// API route for generating dashboard data
// Implementation in Phase 2

import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ message: 'Data endpoint — not yet implemented' });
}
